{
  "name": "containers.css",
  "version": "0.0.0",
  "description": "A set of CSS classes for different kinds of containers",
  "scripts": {
    "dev": "npm run build -- --watch",
    "build": "node-sass --output-style compressed container.scss css/container.min.css"
  },
  "author": "ekawatani",
  "license": "MIT",
  "devDependencies": {
    "node-sass": "^5.0.0"
  },
  "dependencies": {}
}
